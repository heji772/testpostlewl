# ----------------------------------------------------------------------------
# PhishGuard Coupon Platform - Environment Configuration Template
# Copy this file to .env and replace every placeholder before running the stack.
# ----------------------------------------------------------------------------

# Database Configuration
# IMPORTANT: DB_PASSWORD must match the password embedded inside DATABASE_URL.
DATABASE_URL=postgres://phishuser:CHANGE_ME_DB_PASSWORD@database:5432/phishguard
DB_HOST=database
DB_PORT=5432
DB_NAME=phishguard
DB_USER=phishuser
DB_PASSWORD=CHANGE_ME_DB_PASSWORD

# Admin Authentication
# Set ADMIN_USERNAME/ADMIN_PASSWORD to the credentials you want to use.
# Generate a bcrypt hash of ADMIN_PASSWORD and paste it into ADMIN_PASSWORD_HASH.
# Example: docker run --rm node:18-alpine node -e "console.log(require('bcrypt').hashSync(process.argv[1], 12))" "YourPasswordHere"
ADMIN_USERNAME=change_me_admin
ADMIN_PASSWORD=ChangeMe!123
ADMIN_PASSWORD_HASH=PASTE_BCRYPT_HASH_HERE
ADMIN_TOTP_SECRET=
TOTP_APP_NAME=PhishGuard Admin

# Security Keys (generate new values!)
# ENCRYPTION_KEY must be 64 hex characters: openssl rand -hex 32
# JWT_SECRET must be a strong base64 string: openssl rand -base64 32
ENCRYPTION_KEY=REPLACE_WITH_HEX_64
JWT_SECRET=REPLACE_WITH_BASE64_SECRET
REFRESH_TOKEN_TTL_DAYS=30

# Backend Runtime
NODE_ENV=production
PORT=5000

# Network Configuration (change only if the subnet collides with existing networks)
SUBNET=172.20.0.0/24
